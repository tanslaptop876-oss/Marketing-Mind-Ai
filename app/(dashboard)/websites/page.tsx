import { createClient } from "@/lib/supabase/server"; import { addWebsite } from "./actions";
export default async function Websites() { const s=await createClient(); const {data=[]}=await s.from("websites").select("*").order("created_at",{ascending:false}); return <><h1>Websites</h1><p className="muted">Sites you monitor and improve.</p><div className="grid two section"><div className="card"><h2>Add website</h2><form action={addWebsite} className="form"><label>Name<input name="name" required placeholder="Acme Store" /></label><label>Website URL<input name="url" type="url" required placeholder="https://example.com" /></label><label>Industry<input name="industry" placeholder="E-commerce" /></label><button className="btn">Save website</button></form></div><div className="card"><h2>Tracked websites</h2>{!data?.length?<div className="empty">No websites yet.</div>:<table><thead><tr><th>Name</th><th>URL</th><th>Industry</th></tr></thead><tbody>{data.map((w)=><tr key={w.id}><td>{w.name}</td><td><a href={w.url} target="_blank">{w.url}</a></td><td>{w.industry||"—"}</td></tr>)}</tbody></table>}</div></div></> }
